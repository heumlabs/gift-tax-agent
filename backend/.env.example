# 환경 설정 (로컬 개발용)
ENVIRONMENT=local
APP_STAGE=local

# Google Gemini API 키
GOOGLE_API_KEY=your-google-api-key-here

# 데이터베이스 설정 (개별 필드)
DB_HOST=localhost
DB_NAME=shuking
DB_USER=postgres
DB_PASS=password

# 또는 DATABASE_URL로 한번에 설정 가능
# DATABASE_URL=postgresql://postgres:password@localhost:5432/shuking

# CORS 허용 오리진
CORS_ALLOW_ORIGIN=http://localhost:5173
