# Database connection (optional for AI scripts)
DB_HOST=localhost
DB_NAME=shuking
DB_USER=postgres
DB_PASS=postgres

# LLM API
GOOGLE_API_KEY=replace-with-your-key
GEMINI_MODEL=gemini-2.5-flash
GEMINI_TIMEOUT_SECONDS=30.0

# Optional overrides
# APP_STAGE=local
